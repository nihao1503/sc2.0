import { createClient } from '@supabase/supabase-js'; // SERVER-SIDE ONLY. This uses the service role key, which bypasses Row Level // Security entirely — never import this file from anything that runs in the // browser (components, pages' client-side code). It's only safe inside // pages/api/* routes, which run on the server. export function getSupabaseAdmin() { const url = process.env.NEXT_PUBLIC_SUPABASE_URL; const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY; if (!url || !serviceKey) { throw new Error('Missing NEXT_PUBLIC_SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY'); } return createClient(url, serviceKey, { auth: { autoRefreshToken: false, persistSession: false }, }); }
